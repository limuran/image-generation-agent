{
    "name": "image-generation-agent",
    "version": "1.0.0",
    "description": "图像生成Agent - 基于Mastra和Moonshot Kimi",
    "main": "dist/index.js",
    "type": "module",
    "scripts": {
        "dev": "mastra dev",
        "build": "tsc && wrangler deploy --dry-run",
        "deploy": "wrangler deploy",
        "deploy:r2": "wrangler r2 bucket create image-agent-storage",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [
        "ai",
        "agent",
        "image-generation",
        "mastra",
        "cloudflare"
    ],
    "author": "",
    "license": "MIT",
    "dependencies": {
        "@mastra/core": "^0.16.2",
        "@ai-sdk/openai": "^1.0.0",
        "hono": "^4.0.0",
        "zod": "^3.23.0"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20241127.0",
        "@types/node": "^20.0.0",
        "typescript": "^5.3.0",
        "wrangler": "^3.80.0"
    }
}